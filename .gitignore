node_modules/
dist/
test-results/
playwright-report/
.DS_Store

# Secrets: real keys live here, never committed (copy .env.example -> .env)
.env

# Raw AI art you drop in for the optimizer (the optimized sprites are committed instead)
assets/raw/*
!assets/raw/README.md

# Generated by the asset pipeline (reproducible from `npm run pipeline`)
pipeline/agentic/out/
pipeline/agentic/runs/
# dashboard.html IS committed: fully self-contained (data/images inlined) so the LIVE Pages
# dashboard == local. build:pages just copies it (no browser/keys needed in CI).
